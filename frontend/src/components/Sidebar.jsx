export default function Sidebar() {
  return (
    <aside className="sidebar">
      <h2>🌬️</h2>
      <nav>
        <span className="active">Weather</span>
        <span>Cities</span>
        <span>Map</span>
        <span>Settings</span>
      </nav>
    </aside>
  );
}
